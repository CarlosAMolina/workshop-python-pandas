## Virtual environment

Create, activate and install requirements:

```bash
python -m venv env
source env/bin/activate
pip install -r requirements.txt
```

## Run the script

```bash
cd etl
python main.py
```

